const initialState = {
  benchmarkTypes: {},
  benchmarks: {},
  logTypes: {},
  logs: {},
};

const dbReducer = (previous = initialState, action) => {
  return previous;
};

export default dbReducer;
