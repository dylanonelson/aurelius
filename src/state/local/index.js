const initialState = {
  benchmarkTypes: {},
  benchmarks: {},
  logTypes: {},
  logs: {},
};

const uiReducer = (previous = initialState, action) => {
  return previous;
};

export default uiReducer;
