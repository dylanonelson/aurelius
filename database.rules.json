{
  "rules": {
    "logs": {
      "$uid": {
        ".indexOn": "date",
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    },
    "log_types": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    }
  }
}
