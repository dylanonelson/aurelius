{
  "rules": {
    "logs": {
      "$uid": {
        ".indexOn": "date",
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    },
    "logTypes": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid"
      }
    }
  }
}
